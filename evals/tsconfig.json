{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "noEmit": true
  },
  "include": ["datasets/**/*.ts", "harness/**/*.ts", "locomo/**/*.ts", "cli.ts"],
  "exclude": ["node_modules", "datasets/cache", "locomo/cache"]
}
